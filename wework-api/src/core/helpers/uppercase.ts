/* import { SanitizerConstraint, SanitizerInterface } from 'class-sanitizer';

@SanitizerConstraint()
export class UpperCase implements SanitizerInterface {
  sanitize(text: string): string {
    return text.toLocaleUpperCase();
  }
} */
